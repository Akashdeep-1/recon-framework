{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PortRecord",
  "description": "Normalized record representing an open network port discovered on an in-scope host.",
  "type": "object",
  "required": ["host", "port", "protocol", "target"],
  "properties": {
    "host": {
      "type": "string",
      "format": "hostname",
      "description": "Hostname on which the open port was identified."
    },
    "port": {
      "type": "integer",
      "minimum": 1,
      "maximum": 65535,
      "description": "Discovered open TCP or UDP port number."
    },
    "protocol": {
      "type": "string",
      "enum": ["tcp", "udp"],
      "description": "Transport layer protocol."
    },
    "service": {
      "type": "string",
      "description": "Identified service name if banner grabbing or protocol probe succeeded."
    },
    "target": {
      "type": "string",
      "format": "hostname",
      "description": "Root assessment target domain."
    }
  },
  "additionalProperties": true
}
